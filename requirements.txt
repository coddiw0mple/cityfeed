# Vercel's Python runtime installs from this file. It is the pipeline's
# dependencies plus FastAPI -- kept in sync with pyproject.toml by hand, which
# is fine because it changes about once a quarter.
pydantic>=2.0
httpx>=0.27
lxml>=5.0
cssselect>=1.2
PyYAML>=6.0
python-dateutil>=2.9
icalendar>=6.0
feedparser>=6.0
rapidfuzz>=3.0
fastapi>=0.110
