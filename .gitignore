__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
.vercel/
node_modules/

# Crawl snapshots: content-addressed, regenerated on every run, and large.
data/snapshots/

# The built database IS committed -- it is the deployment artefact, and the
# read-only API serves it directly. See .github/workflows/crawl.yml.
!data/cityfeed.db

# Built dashboard output; regenerated by scripts/build_dashboard.py.
public/

# Local tooling config, not project config.
.claude/
.vercel
